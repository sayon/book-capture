# org-book-capture

A simple package to capture book information into an org-file.
You provide a fuzzy search request, the package searches for the book using
Google Books API and provides a list of candidates.
Accept the one that matches a book in your library.
